---
phase: 03-detail-page-business-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content.config.ts
  - src/pages/template/[...id].astro
  - src/components/detail/CtaSection.astro
  - src/components/detail/CredentialBadges.astro
  - src/components/detail/RelatedTemplates.astro
  - src/content/templates/dart-disclosure-monitor.mdx
autonomous: true
requirements: [DETAIL-01, DETAIL-03, DETAIL-04, DETAIL-05, DETAIL-06, BIZ-01, BIZ-02]

must_haves:
  truths:
    - "각 템플릿 상세 페이지(/template/{id})에서 제목, 설명, 사용 시나리오, 난이도, 설정 시간, 노드 수가 표시된다"
    - "무료 템플릿 상세 페이지에서 JSON 다운로드 버튼 클릭 시 파일이 다운로드된다"
    - "프리미엄 템플릿 상세 페이지에서 구매 문의 버튼이 Google Form으로 연결된다"
    - "필요 크레덴셜이 배지 목록으로 표시된다"
    - "MDX body가 설정 가이드(Step 1~3)로 렌더링된다"
    - "하단에 같은 카테고리의 관련 템플릿이 TemplateCard로 표시된다"
  artifacts:
    - path: "src/pages/template/[...id].astro"
      provides: "템플릿 상세 동적 라우트 페이지"
      min_lines: 80
    - path: "src/components/detail/CtaSection.astro"
      provides: "무료/프리미엄 CTA 분기 컴포넌트"
      exports: []
    - path: "src/components/detail/CredentialBadges.astro"
      provides: "크레덴셜 배지 목록 컴포넌트"
      exports: []
    - path: "src/components/detail/RelatedTemplates.astro"
      provides: "관련 템플릿 섹션 컴포넌트"
      exports: []
    - path: "src/content.config.ts"
      provides: "price 필드 추가된 스키마"
      contains: "price"
  key_links:
    - from: "src/pages/template/[...id].astro"
      to: "astro:content"
      via: "getCollection + render"
      pattern: "getCollection.*templates"
    - from: "src/pages/template/[...id].astro"
      to: "src/components/detail/CtaSection.astro"
      via: "import + props"
      pattern: "pricing.*workflowJsonPath"
    - from: "src/components/detail/CtaSection.astro"
      to: "public/workflows/"
      via: "anchor download 속성"
      pattern: "download"
    - from: "src/components/detail/CtaSection.astro"
      to: "Google Form"
      via: "prefill URL"
      pattern: "docs.google.com/forms"
    - from: "src/components/catalog/TemplateCard.astro"
      to: "src/pages/template/[...id].astro"
      via: "href link"
      pattern: "/n8n-marketplace/template/"
---

<objective>
템플릿 상세 페이지의 핵심 구조를 구현한다: 동적 라우트, 메타 배지, MDX body 렌더링, 무료/프리미엄 CTA, 크레덴셜 배지, 관련 템플릿.

Purpose: 사용자가 카탈로그에서 카드를 클릭하면 상세 정보를 확인하고, 무료 템플릿은 즉시 다운로드, 프리미엄은 문의할 수 있는 핵심 사용자 여정을 완성한다.
Output: 템플릿 상세 페이지 + CTA/크레덴셜/관련템플릿 컴포넌트
</objective>

<execution_context>
@C:/Users/a/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/a/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-detail-page-business-model/03-CONTEXT.md
@.planning/phases/03-detail-page-business-model/03-RESEARCH.md

<interfaces>
<!-- 기존 코드에서 추출한 핵심 인터페이스 — executor가 탐색 없이 바로 사용 -->

From src/content.config.ts:
```typescript
// Content Collections 스키마 (현재)
schema: z.object({
  title: z.string(),
  description: z.string(),
  category: z.enum(['finance', 'marketing', 'hr', 'it']),
  useCase: z.string(),
  difficulty: z.enum(['beginner', 'intermediate', 'advanced']),
  pricing: z.enum(['free', 'premium']),
  estimatedMinutes: z.number().positive(),
  credentials: z.array(z.string()),
  tags: z.array(z.string()).default([]),
  workflowJsonPath: z.string(),
  nodeCount: z.number().optional(),
  publishedAt: z.coerce.date(),
  updatedAt: z.coerce.date().optional(),
})
```

From src/layouts/BaseLayout.astro:
```typescript
interface Props {
  title: string;
  description?: string;
  breadcrumbs?: Array<{ label: string; href: string }>;
}
```

From src/components/catalog/TemplateCard.astro:
```typescript
interface Props {
  title: string;
  description: string;
  category: string;
  difficulty: string;
  pricing: string;
  nodeCount?: number;
  estimatedMinutes: number;
  slug: string;
}
// 한글 매핑:
const categoryLabel: Record<string, string> = {
  finance: '금융', marketing: '마케팅', hr: 'HR', it: 'IT/DevOps',
};
const difficultyLabel: Record<string, string> = {
  beginner: '초급', intermediate: '중급', advanced: '고급',
};
```

From src/components/layout/Breadcrumb.astro:
```typescript
interface Props {
  items: Array<{ label: string; href: string }>;
}
// '홈'은 자동으로 첫 번째에 추가됨
```

From astro.config.mjs:
```javascript
base: '/n8n-marketplace'
// 모든 경로에 /n8n-marketplace prefix가 붙음
// import.meta.env.BASE_URL로 접근
```

From TemplateCard link pattern:
```html
<a href={`/n8n-marketplace/template/${slug}`}>
<!-- slug에는 template.id가 전달됨 -->
```

From MDX 템플릿 예시 (dart-disclosure-monitor.mdx):
```yaml
pricing: "premium"
credentials:
  - "DART API Key"
  - "Slack OAuth"
workflowJsonPath: "/workflows/dart-disclosure-monitor.json"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Content Collections 스키마 업데이트 + 상세 컴포넌트 생성</name>
  <files>src/content.config.ts, src/components/detail/CtaSection.astro, src/components/detail/CredentialBadges.astro, src/components/detail/RelatedTemplates.astro, src/content/templates/dart-disclosure-monitor.mdx</files>
  <action>
1. **src/content.config.ts** — 스키마에 `price: z.number().optional()` 필드 추가. premium 템플릿의 가격(원) 표시용. 기존 필드는 변경하지 않는다.

2. **src/content/templates/dart-disclosure-monitor.mdx** — frontmatter에 `price: 29000` 추가 (프리미엄 템플릿 가격 테스트용). MDX body에 설정 가이드 섹션 추가:
```markdown
## 설정 가이드

### Step 1: 크레덴셜 발급
DART Open API에서 API 키를 발급받습니다...

### Step 2: n8n 설정
다운로드한 JSON을 n8n에 Import하고 크레덴셜을 설정합니다...

### Step 3: 활성화
워크플로우를 활성화하면 자동으로 공시를 모니터링합니다...
```

3. **src/components/detail/CtaSection.astro** — 무료/프리미엄 분기 CTA 컴포넌트.
   - Props: `{ pricing: string; workflowJsonPath: string; title: string; price?: number }`
   - 무료(pricing === 'free'): `<a href={base + workflowJsonPath} download={title + '.json'}>` 형태의 다운로드 버튼. bg-primary-500 스타일. 다운로드 아이콘 SVG 포함.
   - 프리미엄: `<a href={googleFormPrefillUrl} target="_blank">` 형태의 구매 문의 버튼. bg-amber-500 스타일. price가 있으면 `₩{price.toLocaleString()} · 구매 문의하기`, 없으면 `프리미엄 · 구매 문의하기` 텍스트.
   - Google Form URL은 상수로 분리: `const GOOGLE_FORM_URL = 'https://docs.google.com/forms/d/e/PLACEHOLDER_FORM_ID/viewform';` 플레이스홀더 사용, TODO 코멘트로 실제 Form ID 교체 안내.
   - `const TEMPLATE_NAME_ENTRY = 'entry.123456789';` 플레이스홀더, TODO 코멘트.
   - prefill URL: `${GOOGLE_FORM_URL}?usp=pp_url&${TEMPLATE_NAME_ENTRY}=${encodeURIComponent(title)}`
   - `import.meta.env.BASE_URL`로 base path 처리.

4. **src/components/detail/CredentialBadges.astro** — 크레덴셜 배지 목록.
   - Props: `{ credentials: string[] }`
   - 각 크레덴셜을 `bg-surface-100 text-surface-600 px-3 py-1 rounded-full text-sm` 배지로 표시.
   - 앞에 자물쇠 아이콘 SVG 추가 (크레덴셜=인증 정보 시각적 암시).

5. **src/components/detail/RelatedTemplates.astro** — 관련 템플릿 섹션.
   - Props: `{ templates: Array<{id: string; data: {title: string; description: string; category: string; difficulty: string; pricing: string; nodeCount?: number; estimatedMinutes: number}}> }`
   - 같은 카테고리의 다른 템플릿 최대 3개를 TemplateCard 컴포넌트로 표시.
   - templates 배열이 비어있으면 섹션 자체를 렌더링하지 않는다.
   - 섹션 제목: "관련 템플릿"
   - grid-cols-1 md:grid-cols-3 레이아웃.
  </action>
  <verify>
    <automated>cd /d/PARA/Resource/2026-zion-spring-retreat/n8n-maker && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>스키마에 price 필드 추가, 3개 상세 컴포넌트(CtaSection, CredentialBadges, RelatedTemplates) 생성 완료, 빌드 성공</done>
</task>

<task type="auto">
  <name>Task 2: 템플릿 상세 페이지 동적 라우트 생성</name>
  <files>src/pages/template/[...id].astro</files>
  <action>
`src/pages/template/[...id].astro` 생성 — 단일 컨텐츠 플로우 구조 (사용자 결정 준수):

**CRITICAL Astro 5 변경사항:**
- `import { getCollection, render } from 'astro:content'` 사용 (entry.render() 아님)
- params에 `id: template.id` 사용 (slug 아님)
- 파일명은 `[...id].astro` (rest parameter)

**레이아웃 구조 (Locked Decision — 정확히 이 순서 준수):**
1. **Breadcrumb**: BaseLayout의 breadcrumbs prop으로 전달. items: `[{label: categoryLabel[category], href: '/n8n-marketplace/category/' + category}, {label: title, href: ''}]`
2. **제목 + 메타 배지 라인**: h1 제목. 아래 한 줄에 컴팩트 배지 — 난이도(surface), 설정시간(surface), 노드수(surface), 가격(green/amber). TemplateCard의 배지 패턴(색상, 크기) 동일 적용.
3. **상단 CTA**: CtaSection 컴포넌트 (헤더 바로 아래 위치에 해당)
4. **다이어그램 영역**: 지금은 플레이스홀더 div만 배치 — `<div id="workflow-diagram" class="rounded-xl border border-surface-200 bg-surface-50 h-[300px] md:h-[450px] flex items-center justify-center"><p class="text-surface-400">워크플로우 다이어그램 (Plan 03에서 구현)</p></div>`. Plan 03에서 WorkflowDiagram 컴포넌트로 교체.
5. **설명 (MDX body)**: `<Content />` 컴포넌트 렌더링. prose 스타일링 적용 — `<article class="prose prose-surface max-w-none">`. Tailwind Typography가 없으므로 직접 스타일링: `prose` 클래스 대신 MDX 렌더링 영역에 `[&_h2]:text-xl [&_h2]:font-bold [&_h2]:mt-8 [&_h2]:mb-4 [&_h3]:text-lg [&_h3]:font-semibold [&_h3]:mt-6 [&_h3]:mb-3 [&_p]:text-surface-600 [&_p]:mb-4 [&_p]:leading-relaxed [&_ul]:list-disc [&_ul]:pl-5 [&_ul]:mb-4 [&_li]:text-surface-600 [&_li]:mb-1` Tailwind arbitrary selector 사용.
6. **크레덴셜 배지**: CredentialBadges 컴포넌트. "필요한 크레덴셜" 소제목과 함께 배지 목록 표시.
7. **하단 CTA**: CtaSection 반복 (페이지 바닥, 상단과 동일 컴포넌트 재사용).
8. **관련 템플릿**: RelatedTemplates 컴포넌트. 같은 카테고리에서 현재 템플릿 제외 최대 3개.

**getStaticPaths:**
```typescript
export async function getStaticPaths() {
  const allTemplates = await getCollection('templates');
  return allTemplates.map((template) => ({
    params: { id: template.id },
    props: { template, allTemplates },
  }));
}
```

**한글 매핑:** TemplateCard와 동일한 categoryLabel/difficultyLabel 매핑을 페이지 내부에 정의 (TemplateCard에서 import 불가 — Astro 컴포넌트 export 제약).

**base URL 처리:** `import.meta.env.BASE_URL`을 사용하여 워크플로우 JSON 경로 조합. CTA 컴포넌트에 전달.

**전체 페이지 레이아웃:** max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-8. 섹션 간 간격 mt-8~mt-12.
  </action>
  <verify>
    <automated>cd /d/PARA/Resource/2026-zion-spring-retreat/n8n-maker && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>템플릿 상세 페이지가 3개 템플릿 모두에 대해 정적 생성되고, Breadcrumb + 메타 배지 + CTA + MDX body + 크레덴셜 + 관련 템플릿이 단일 플로우로 표시된다. TemplateCard에서 클릭하면 상세 페이지로 정상 이동한다.</done>
</task>

</tasks>

<verification>
- `npm run build` 성공 (페이지 정적 생성 확인)
- 빌드 출력에 `/template/dart-disclosure-monitor/`, `/template/ai-email-classifier/`, `/template/job-auto-collect/` 3개 페이지 생성 확인
- TemplateCard의 href(`/n8n-marketplace/template/{id}`)와 상세 페이지 라우트 경로 일치 확인
</verification>

<success_criteria>
- 3개 템플릿 상세 페이지가 빌드 시 정적 생성된다
- 각 페이지에 Breadcrumb, 메타 배지(난이도/시간/노드수/가격), CTA(상단+하단), MDX body, 크레덴셜 배지, 관련 템플릿이 포함된다
- 무료 템플릿 CTA에 download 속성이 있는 다운로드 링크가 있다
- 프리미엄 템플릿 CTA에 Google Form prefill URL 링크가 있다
</success_criteria>

<output>
완료 후 `.planning/phases/03-detail-page-business-model/03-01-SUMMARY.md` 생성
</output>
