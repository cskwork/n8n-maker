---
phase: 03-detail-page-business-model
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/pricing.astro
autonomous: true
requirements: [BIZ-03]

must_haves:
  truths:
    - "/pricing 페이지에 프리미엄 가격 범위, 포함 사항, Google Form 문의 링크가 표시된다"
  artifacts:
    - path: "src/pages/pricing.astro"
      provides: "프리미엄 안내 및 가격 페이지"
      min_lines: 60
  key_links:
    - from: "src/pages/pricing.astro"
      to: "Google Form"
      via: "문의 링크"
      pattern: "docs.google.com/forms"
    - from: "src/pages/pricing.astro"
      to: "src/layouts/BaseLayout.astro"
      via: "import BaseLayout"
      pattern: "BaseLayout"
---

<objective>
프리미엄 안내 /pricing 페이지를 생성한다.

Purpose: 프리미엄 템플릿에 관심 있는 사용자가 가격, 포함 사항, 문의 방법을 한눈에 파악할 수 있는 독립 페이지를 제공한다.
Output: /pricing 정적 페이지
</objective>

<execution_context>
@C:/Users/a/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/a/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-detail-page-business-model/03-CONTEXT.md

<interfaces>
<!-- 기존 코드에서 추출한 핵심 인터페이스 -->

From src/layouts/BaseLayout.astro:
```typescript
interface Props {
  title: string;
  description?: string;
  breadcrumbs?: Array<{ label: string; href: string }>;
}
```

From astro.config.mjs:
```javascript
base: '/n8n-marketplace'
```

기존 페이지 스타일 패턴 (index.astro):
- 섹션: py-16 md:py-24
- 컨테이너: max-w-7xl mx-auto px-4 sm:px-6 lg:px-8
- 제목: text-2xl md:text-3xl font-bold text-surface-900
- 본문: text-surface-600
- CTA 버튼: px-6 py-3 bg-primary-500 text-white font-semibold rounded-lg
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: /pricing 프리미엄 안내 페이지 생성</name>
  <files>src/pages/pricing.astro</files>
  <action>
`src/pages/pricing.astro` 생성 — 프리미엄 템플릿 안내 페이지.

**Breadcrumb:** `breadcrumbs={[{label: '가격 안내', href: ''}]}`

**페이지 구조 (Claude's Discretion — 레이아웃/문구):**

1. **히어로 섹션**: "프리미엄 워크플로우 템플릿" 제목 + "전문가가 설계한 고급 자동화 템플릿으로 업무를 혁신하세요" 부제목.

2. **무료 vs 프리미엄 비교 테이블/카드**: 2컬럼 카드 레이아웃 (모바일 1컬럼).
   - **무료**: 기본 워크플로우, 커뮤니티 가이드, 즉시 다운로드. 가격: "무료". CTA: "무료 템플릿 보기" -> `/n8n-marketplace/`
   - **프리미엄**: 고급 워크플로우, 1:1 커스터마이징 가이드, 이메일 지원, 에러 해결 포함. 가격 범위: "₩19,000 ~ ₩49,000". CTA: "구매 문의하기" -> Google Form URL.
   - 프리미엄 카드에 "추천" 배지 또는 강조 border.

3. **프리미엄 포함 사항 상세**: 아이콘 + 텍스트 리스트.
   - 워크플로우 JSON 파일 + 상세 설정 가이드
   - 1:1 커스터마이징 컨설팅 (이메일)
   - 에러 발생 시 해결 지원
   - 업데이트 시 무료 재다운로드

4. **FAQ 섹션** (선택): 2-3개 질문.
   - "프리미엄 템플릿은 어떻게 받나요?" -> Google Form으로 문의 후 이메일로 전달
   - "환불이 가능한가요?" -> 디지털 상품 특성상 제한적, 문의 시 협의

5. **하단 CTA**: "궁금한 점이 있으신가요?" + Google Form 문의 링크 버튼.

**Google Form URL:** `const GOOGLE_FORM_URL = 'https://docs.google.com/forms/d/e/PLACEHOLDER_FORM_ID/viewform';` 플레이스홀더 사용. TODO 코멘트로 실제 Form ID 교체 안내.

**스타일링:** 기존 사이트 디자인 시스템 일관성 유지 — n8n 브랜드 컬러(primary), surface 뉴트럴, Pretendard 폰트. 프리미엄 카드에는 amber 계열 강조.
  </action>
  <verify>
    <automated>cd /d/PARA/Resource/2026-zion-spring-retreat/n8n-maker && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>/pricing 페이지가 빌드되고, 무료/프리미엄 비교, 포함 사항, Google Form 문의 링크가 포함된다</done>
</task>

</tasks>

<verification>
- `npm run build` 성공
- 빌드 출력에 `/pricing/` 페이지 생성 확인
</verification>

<success_criteria>
- /pricing 페이지가 빌드 시 정적 생성된다
- 무료 vs 프리미엄 비교가 시각적으로 명확하다
- Google Form 문의 링크(플레이스홀더)가 포함된다
- BaseLayout + Breadcrumb 일관성 유지
</success_criteria>

<output>
완료 후 `.planning/phases/03-detail-page-business-model/03-02-SUMMARY.md` 생성
</output>
