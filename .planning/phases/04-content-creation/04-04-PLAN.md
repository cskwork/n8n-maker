---
phase: 04-content-creation
plan: 04
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
  - 04-03
files_modified: []
autonomous: false
requirements:
  - CONT-01
  - CONT-02
  - CONT-03
  - CONT-04
  - CONT-05
  - CONT-06

must_haves:
  truths:
    - "전체 18개 템플릿(기존 3 + 신규 15)이 모두 빌드 성공하고 카테고리별로 올바르게 분배된다"
    - "4개 한국 서비스 API(DART, 사람인, 네이버, 카카오톡) 설정 가이드가 해당 템플릿 MDX에 포함된다"
    - "모든 JSON 파일의 connections 키가 노드 name과 일치하고, settings.timezone이 Asia/Seoul이다"
    - "카테고리별 무료/프리미엄 분배가 균형적이다 (각 카테고리 무료 2개 이상)"
  artifacts: []
  key_links:
    - from: "src/content/templates/*.mdx"
      to: "public/workflows/*.json"
      via: "workflowJsonPath 프론트매터"
      pattern: "workflowJsonPath"
---

<objective>
Phase 4 전체 콘텐츠를 종합 검증한다. 빌드 성공, 카테고리별 템플릿 수, JSON 유효성, 한국 API 가이드 존재 여부를 확인하고, 시각적으로 사이트를 검증한다.

Purpose: 모든 CONT 요구사항이 충족되었는지 종합 확인
Output: Phase 4 검증 완료 확인
</objective>

<execution_context>
@C:/Users/a/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/a/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-content-creation/04-RESEARCH.md
@.planning/phases/04-content-creation/04-01-SUMMARY.md
@.planning/phases/04-content-creation/04-02-SUMMARY.md
@.planning/phases/04-content-creation/04-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 전체 빌드 검증 + 콘텐츠 감사</name>
  <files></files>
  <action>
1. `npm run build` 실행하여 전체 빌드 성공 확인

2. 카테고리별 템플릿 수 확인:
   - finance: 5개 (dart-disclosure-monitor, stock-price-monitor, financial-news-summary, invoice-auto-process, expense-report-auto)
   - marketing: 5개 (kakao-schedule-notify, sns-cross-posting, competitor-monitor, naver-blog-auto-post, ai-content-pipeline)
   - hr: 4개 (job-auto-collect, daily-work-report, saramin-ai-resume, onboarding-automation)
   - it: 4개 (ai-email-classifier, naver-news-monitor, server-health-monitor, slack-notion-task-sync)
   - 총 18개 확인

3. 카테고리별 무료/프리미엄 분배 확인:
   - 각 카테고리에 무료 2개 이상 존재하는지 확인 (BIZ-01 호환)

4. 한국서비스 태그 템플릿 확인:
   - tags에 "한국서비스" 포함된 템플릿 수 카운트 (최소 6개: dart + job-auto-collect + kakao + naver-news + naver-blog + saramin)

5. 한국 API 설정 가이드 확인 (CONT-06):
   - DART: dart-disclosure-monitor.mdx에 "설정 가이드" 섹션 존재 확인 (기존)
   - 사람인: saramin-ai-resume.mdx에 "설정 가이드" 섹션 + API 발급 안내 존재 확인
   - 네이버: naver-news-monitor.mdx에 "설정 가이드" 섹션 + Client ID/Secret 안내 존재 확인
   - 카카오톡: kakao-schedule-notify.mdx에 "설정 가이드" 섹션 + Access Token 안내 존재 확인

6. JSON 유효성 검증:
   - 모든 JSON 파일에 대해 `node -e "JSON.parse(require('fs').readFileSync('파일경로','utf8'))"` 실행
   - 각 JSON에 nodes, connections, settings 필드 존재 확인
   - settings.timezone이 모두 "Asia/Seoul"인지 확인

7. MDX-JSON 링크 검증:
   - 각 MDX의 workflowJsonPath가 가리키는 JSON 파일이 public/workflows/에 실제 존재하는지 확인

오류 발견 시 즉시 수정. 누락된 파일이 있으면 해당 Plan의 SUMMARY를 참조하여 보충.
  </action>
  <verify>
    <automated>cd "D:/PARA/Resource/2026-zion-spring-retreat/n8n-maker" && npm run build 2>&1 | tail -5 && echo "---" && ls src/content/templates/*.mdx | wc -l && echo "MDX files" && ls public/workflows/*.json | wc -l && echo "JSON files"</automated>
  </verify>
  <done>18개 MDX + 18개 JSON 전체 빌드 성공, 카테고리별 분배 정상, 한국 API 가이드 4개 존재, JSON 유효성 통과</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Phase 4 시각 검증</name>
  <files></files>
  <action>
Phase 4 전체 콘텐츠 시각 검증: 15개 신규 워크플로우 템플릿 (금융 4 + 마케팅 5 + HR 3 + IT 3) + 한국 서비스 API 설정 가이드 4개.

`npm run preview` 실행 후 사용자에게 검증 요청:
1. http://localhost:4321 접속
2. 랜딩 페이지에서 카테고리별 템플릿 수 확인 (금융 5, 마케팅 5, HR 4, IT 4)
3. 금융 카테고리 -> 5개 템플릿 카드 표시, 무료/프리미엄 배지 확인
4. "주가 모니터링 알리미" 상세 -> 설명, 크레덴셜, 다운로드 버튼, 다이어그램
5. 마케팅 -> "카카오톡 일정 알림" 상세 -> 카카오 API 설정 가이드 한글 확인
6. HR -> "사람인 AI 자기소개서" 상세 -> 프리미엄 표시 + 사람인 API 가이드
7. IT -> "네이버 뉴스 모니터링" 상세 -> 네이버 API 가이드
8. 무료 템플릿 JSON 다운로드 -> 유효한 JSON 파일 확인
  </action>
  <verify>"approved" 또는 발견된 문제 설명</verify>
  <done>사용자 시각 검증 approved, Phase 4 전체 콘텐츠 완료</done>
</task>

</tasks>

<verification>
- `npm run build && npm run preview` 정상 동작
- 4개 카테고리에 총 18개 템플릿 표시
- 한국 서비스 API 가이드 4개 (DART, 사람인, 네이버, 카카오톡) 확인
- JSON 다운로드 정상 동작
- 시각적 UI 검증 완료
</verification>

<success_criteria>
- 전체 빌드 성공
- 카테고리별 템플릿 수: finance 5, marketing 5, hr 4, it 4
- 한국 API 가이드 4종 한글 포함
- 사용자 시각 검증 approved
</success_criteria>

<output>
After completion, create `.planning/phases/04-content-creation/04-04-SUMMARY.md`
</output>
