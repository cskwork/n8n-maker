---
phase: 02-카탈로그-랜딩-UI
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/pages/index.astro
autonomous: false
requirements: [CATA-04]

must_haves:
  truths:
    - "히어로 섹션 CTA 아래에 통계 수치가 가로 인라인으로 표시된다 (N+ 템플릿 · 4개 업종 · 즉시 다운로드)"
    - "인기 템플릿 섹션에 4개 카테고리에서 각 1개씩 균형적으로 표시된다 (없는 카테고리는 건너뜀)"
    - "인기 템플릿이 TemplateCard 컴포넌트로 렌더링된다 (index.astro 인라인 카드 대체)"
    - "카테고리 카드가 모바일에서 2x2 그리드로 표시된다"
  artifacts:
    - path: "src/pages/index.astro"
      provides: "개선된 랜딩 페이지"
      contains: "TemplateCard"
  key_links:
    - from: "src/pages/index.astro"
      to: "src/components/catalog/TemplateCard.astro"
      via: "인기 템플릿 섹션에서 TemplateCard import 사용"
      pattern: "import TemplateCard"
    - from: "src/pages/index.astro"
      to: "astro:content getCollection"
      via: "카테고리별 첫 번째 템플릿 선택 로직"
      pattern: "categoryOrder.*map.*find"
---

<objective>
index.astro 랜딩 페이지 개선 - 히어로 통계 추가, 인기 템플릿 TemplateCard 전환, 모바일 2x2 카테고리

Purpose: 사용자가 랜딩 페이지에서 마켓플레이스 전체를 한눈에 파악할 수 있게 한다
Output: 개선된 index.astro (TemplateCard 활용 + 히어로 통계 + 모바일 최적화)
</objective>

<execution_context>
@C:/Users/a/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/a/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-카탈로그-랜딩-UI/02-CONTEXT.md
@.planning/phases/02-카탈로그-랜딩-UI/02-RESEARCH.md
@.planning/phases/02-카탈로그-랜딩-UI/02-01-SUMMARY.md

<interfaces>
<!-- Plan 01에서 생성된 TemplateCard 인터페이스 -->

From src/components/catalog/TemplateCard.astro (Plan 01에서 생성):
```typescript
interface Props {
  title: string;
  description: string;
  category: string;
  difficulty: string;
  pricing: string;
  nodeCount?: number;
  estimatedMinutes: number;
  slug: string;
}
// categoryLabel, difficultyLabel 매핑이 컴포넌트 내부에 정의됨
// 카드 전체가 <a href="/n8n-marketplace/template/${slug}"> 링크
```

From src/pages/index.astro (현재 상태 - 수정 대상):
```typescript
// 현재: const sampleTemplates = allTemplates.slice(0, 3);
// 변경: 카테고리별 첫 번째 템플릿 선택 로직으로 교체

// 현재: categoryLabel, difficultyLabel 인라인 정의
// 변경: TemplateCard가 내부에 매핑을 갖고 있으므로 제거

// 현재: 카테고리 그리드 grid-cols-1 md:grid-cols-2 lg:grid-cols-4
// 변경: grid-cols-2 lg:grid-cols-4 (모바일 2x2)
```

기존 base path: `/n8n-marketplace/`
기존 섹션 순서: 히어로 -> 카테고리 -> 인기 템플릿 -> CTA (유지)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: index.astro 랜딩 페이지 개선 (히어로 통계 + TemplateCard + 모바일 2x2)</name>
  <files>src/pages/index.astro</files>
  <action>
기존 src/pages/index.astro를 수정한다. 섹션 배치 순서(히어로 -> 카테고리 -> 인기 템플릿 -> CTA)는 유지한다.

**1. TemplateCard import 추가:**
- `import TemplateCard from '../components/catalog/TemplateCard.astro';`

**2. frontmatter 로직 변경:**
- 기존 `const sampleTemplates = allTemplates.slice(0, 3);` 제거
- 인기 템플릿 카테고리별 균형 선택 로직 추가:
  ```typescript
  const categoryOrder = ['finance', 'marketing', 'hr', 'it'];
  const featuredTemplates = categoryOrder
    .map((cat) => allTemplates.find((t) => t.data.category === cat))
    .filter(Boolean);
  ```
- categoryLabel, difficultyLabel 매핑 제거 (TemplateCard 내부에 있으므로 불필요)

**3. 히어로 섹션 수정:**
- CTA 버튼(`<a href="#categories">`) 아래에 통계 수치 추가 (사용자 결정: CTA 아래 가로 인라인):
  ```html
  <p class="mt-4 text-sm text-surface-400">
    {allTemplates.length}+ 템플릿 · 4개 업종 · 즉시 다운로드
  </p>
  ```
- 구체적 스타일링은 Claude 재량

**4. 카테고리 섹션 모바일 2x2 수정:**
- 기존: `grid-cols-1 md:grid-cols-2 lg:grid-cols-4`
- 변경: `grid-cols-2 lg:grid-cols-4` (사용자 결정: 모바일에서 2x2 그리드로 컴팩트하게)
- gap도 조정 가능: `gap-4 md:gap-6` (작은 화면에서 간격 축소)

**5. 인기 템플릿 섹션 TemplateCard 전환:**
- 기존 인라인 카드 마크업(sampleTemplates.map) 전체를 TemplateCard 컴포넌트로 교체
- featuredTemplates.map으로 변경
- 그리드 유지: `grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6`
  (4개가 표시되면 lg:grid-cols-4도 고려 가능 -- Claude 재량)
- TemplateCard에 props 전달: title, description, category, difficulty, pricing, nodeCount, estimatedMinutes, slug(=t.id)

**6. CTA 섹션:** 변경 없음 (현재 구조 유지)

**주의사항:**
- 카테고리 카드의 SVG 아이콘 코드는 그대로 유지 (Phase 1 체크포인트에서 교체한 것)
- categories 배열도 그대로 유지 (카테고리 카드에서 사용)
  </action>
  <verify>
    <automated>cd "D:/PARA/Resource/2026-zion-spring-retreat/n8n-maker" && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>npm run build 에러 없이 완료. index.astro가 TemplateCard를 import하고, 히어로에 통계 텍스트가 있고, 카테고리 그리드가 grid-cols-2로 시작하고, 인기 템플릿이 카테고리별 균형 선택 로직을 사용한다</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Phase 2 전체 UI 시각 검증</name>
  <files>없음 (검증 전용)</files>
  <action>
`npm run preview`로 로컬 서버를 실행하고 사용자에게 다음 검증을 요청한다:

Phase 2에서 구현된 전체 UI (카테고리 페이지 4개 + 랜딩 페이지 개선)를 시각적으로 확인한다.

**검증 항목:**
1. 랜딩 페이지 확인:
   - 히어로 섹션 CTA 아래에 "N+ 템플릿 · 4개 업종 · 즉시 다운로드" 통계 표시 여부
   - 카테고리 카드가 모바일(375px)에서 2x2 그리드로 표시되는지
   - 인기 템플릿 섹션에 카테고리별 균형적 카드 표시 여부
2. 카테고리 페이지 확인 (헤더의 금융/마케팅/HR/IT 링크 클릭):
   - 금융: 카테고리명 + 설명 + 카운트 + 템플릿 카드 1개 표시
   - 마케팅: "준비 중입니다" 안내 + 다른 카테고리 링크
   - HR: 카드 1개 표시
   - IT: 카드 1개 표시
3. 카드 호버/탭 효과 동작 확인
4. 정렬 드롭다운 존재 확인

**재개 신호:** "approved" 또는 수정 요청 사항을 설명
  </action>
  <verify>사용자가 "approved"로 응답하거나 피드백을 제공</verify>
  <done>사용자가 Phase 2 전체 UI를 시각적으로 승인함</done>
</task>

</tasks>

<verification>
1. `npm run build` 에러 없이 완료
2. dist/index.html에 통계 텍스트("템플릿 · 4개 업종")가 포함됨
3. dist/index.html에 TemplateCard 렌더링 결과(template/ 링크)가 포함됨
4. dist/category/ 4개 페이지 정상 빌드
5. 사용자 시각 검증 통과
</verification>

<success_criteria>
- 랜딩 페이지 히어로에 통계 수치가 표시되고 카테고리 카드가 모바일 2x2로 렌더링됨
- 인기 템플릿이 TemplateCard 컴포넌트로 카테고리별 균형적으로 표시됨
- 사용자가 전체 UI를 시각적으로 승인함
</success_criteria>

<output>
완료 후 `.planning/phases/02-카탈로그-랜딩-UI/02-02-SUMMARY.md` 생성
</output>
