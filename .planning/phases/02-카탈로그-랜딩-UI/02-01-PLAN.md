---
phase: 02-카탈로그-랜딩-UI
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/catalog/TemplateCard.astro
  - src/components/catalog/SortDropdown.astro
  - src/components/catalog/EmptyCategory.astro
  - src/pages/category/[slug].astro
autonomous: true
requirements: [CATA-01, CATA-02, CATA-03]

must_haves:
  truths:
    - "4개 업종 카테고리(finance, marketing, hr, it) 페이지가 각각 /n8n-marketplace/category/{slug}에 빌드된다"
    - "카테고리 페이지에서 해당 카테고리의 템플릿이 카드형 그리드(3/2/1칸)로 표시된다"
    - "템플릿 카드에 제목, 설명(2줄), 카테고리/난이도/가격 배지, 노드 수 + 설정 시간이 표시된다"
    - "빈 카테고리(마케팅)에서 '준비 중입니다' 안내와 다른 카테고리 링크가 표시된다"
    - "정렬 드롭다운으로 난이도순/최신순/가격순 클라이언트 정렬이 동작한다"
  artifacts:
    - path: "src/components/catalog/TemplateCard.astro"
      provides: "재사용 가능한 템플릿 카드 컴포넌트"
      min_lines: 30
    - path: "src/components/catalog/SortDropdown.astro"
      provides: "클라이언트 사이드 정렬 드롭다운"
      min_lines: 10
    - path: "src/components/catalog/EmptyCategory.astro"
      provides: "빈 카테고리 안내 컴포넌트"
      min_lines: 15
    - path: "src/pages/category/[slug].astro"
      provides: "카테고리 동적 라우트 페이지"
      min_lines: 40
    - path: "dist/category/finance/index.html"
      provides: "빌드된 금융 카테고리 페이지"
    - path: "dist/category/marketing/index.html"
      provides: "빌드된 마케팅 카테고리 페이지"
  key_links:
    - from: "src/pages/category/[slug].astro"
      to: "astro:content getCollection"
      via: "getStaticPaths에서 카테고리별 필터링"
      pattern: "getCollection.*templates.*filter.*category"
    - from: "src/pages/category/[slug].astro"
      to: "src/components/catalog/TemplateCard.astro"
      via: "templates.map으로 카드 렌더링"
      pattern: "TemplateCard"
    - from: "src/components/catalog/TemplateCard.astro"
      to: "/n8n-marketplace/template/{slug}"
      via: "카드 클릭 시 상세 페이지 링크"
      pattern: "href.*template"
---

<objective>
카테고리 동적 라우트 페이지(category/[slug].astro)와 재사용 카탈로그 컴포넌트(TemplateCard, SortDropdown, EmptyCategory) 생성

Purpose: 사용자가 4개 업종 카테고리로 템플릿을 브라우징하고, 카드형 레이아웃으로 정보를 한눈에 파악할 수 있게 한다
Output: 4개 카테고리 정적 페이지 + 3개 재사용 컴포넌트
</objective>

<execution_context>
@C:/Users/a/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/a/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-카탈로그-랜딩-UI/02-CONTEXT.md
@.planning/phases/02-카탈로그-랜딩-UI/02-RESEARCH.md

<interfaces>
<!-- Phase 1에서 생성된 기존 인터페이스. 이 컴포넌트/타입을 직접 사용한다. -->

From src/content.config.ts:
```typescript
// Content Collections 스키마 (12필드)
schema: z.object({
  title: z.string(),
  description: z.string(),
  category: z.enum(['finance', 'marketing', 'hr', 'it']),
  useCase: z.string(),
  difficulty: z.enum(['beginner', 'intermediate', 'advanced']),
  pricing: z.enum(['free', 'premium']),
  estimatedMinutes: z.number().positive(),
  credentials: z.array(z.string()),
  tags: z.array(z.string()).default([]),
  workflowJsonPath: z.string(),
  nodeCount: z.number().optional(),
  publishedAt: z.coerce.date(),
})
```

From src/layouts/BaseLayout.astro:
```typescript
interface Props {
  title: string;
  description?: string;
  breadcrumbs?: Array<{ label: string; href: string }>;
}
```

From src/components/layout/Breadcrumb.astro:
```typescript
interface Props {
  items: Array<{ label: string; href: string }>;
}
// 홈 항목은 자동으로 첫 번째에 추가됨
```

From src/pages/index.astro (기존 매핑 — TemplateCard로 이동할 것):
```typescript
const categoryLabel: Record<string, string> = {
  finance: '금융', marketing: '마케팅', hr: 'HR', it: 'IT/DevOps',
};
const difficultyLabel: Record<string, string> = {
  beginner: '초급', intermediate: '중급', advanced: '고급',
};
```

기존 base path 패턴: `/n8n-marketplace/` (모든 href에 반영)
기존 브레이크포인트: md(768px)에서 모바일/데스크톱 전환
현재 템플릿 3개: finance(1개), hr(1개), it(1개), marketing(0개)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TemplateCard + EmptyCategory + SortDropdown 카탈로그 컴포넌트 생성</name>
  <files>src/components/catalog/TemplateCard.astro, src/components/catalog/SortDropdown.astro, src/components/catalog/EmptyCategory.astro</files>
  <action>
src/components/catalog/ 디렉토리에 3개 컴포넌트를 생성한다.

**TemplateCard.astro:**
- Props: { title, description, category, difficulty, pricing, nodeCount?, estimatedMinutes, slug }
- 카드 전체가 `<a href="/n8n-marketplace/template/${slug}">` 링크 (Phase 3에서 구현될 상세 페이지)
- 정보 배치 순서: 제목 -> 설명(line-clamp-2) -> 배지들(카테고리/난이도/가격) -> 보조 정보(노드 수 + 설정 시간)
- 배지 스타일: 카테고리(bg-primary-50 text-primary-700), 난이도(bg-surface-100 text-surface-600), 가격(무료: bg-green-50 text-green-700 / 프리미엄: bg-amber-50 text-amber-700) -- 현재 index.astro 패턴 유지
- 보조 정보: "6개 노드 · 25분" 형식 (nodeCount가 있을 때만 노드 수 표시)
- 카드 스타일: bg-white rounded-xl border border-surface-200 p-6
- 호버: hover:shadow-lg hover:border-primary-300 transition-all (Claude 재량)
- 모바일 탭 피드백: active:scale-[0.98] active:transition-transform (사용자 결정)
- categoryLabel, difficultyLabel 매핑을 이 컴포넌트 내부에 정의 (index.astro에서 중복 제거를 Plan 02에서 처리)

**SortDropdown.astro:**
- select 요소: id="sort-select"
- 옵션 3개: 난이도순(difficulty, 기본), 최신순(date), 가격순(pricing)
- 스타일: border border-surface-200 rounded-lg px-3 py-2 text-sm text-surface-700 (Claude 재량)
- 클라이언트 `<script>` 포함: select 변경 시 id="template-grid" 내부 children을 data-difficulty/data-date/data-pricing 속성으로 재정렬
- DOM 재정렬 방식: [...grid.children].sort() 후 forEach appendChild (RESEARCH.md Pattern 3 참조)

**EmptyCategory.astro:**
- Props: { currentSlug: string }
- "준비 중입니다" 안내 메시지 (사용자 결정)
- 다른 카테고리 링크 목록 (currentSlug 제외한 나머지 3개)
- 카테고리 목록: { finance: '금융', marketing: '마케팅', hr: 'HR', it: 'IT/DevOps' } 에서 currentSlug 필터링
- href: /n8n-marketplace/category/{slug} (base path 포함 필수)
  </action>
  <verify>
    <automated>cd "D:/PARA/Resource/2026-zion-spring-retreat/n8n-maker" && ls src/components/catalog/TemplateCard.astro src/components/catalog/SortDropdown.astro src/components/catalog/EmptyCategory.astro 2>&1</automated>
  </verify>
  <done>3개 카탈로그 컴포넌트 파일이 존재하고, TemplateCard에 categoryLabel/difficultyLabel 매핑이 포함되며, SortDropdown에 클라이언트 정렬 script가 포함된다</done>
</task>

<task type="auto">
  <name>Task 2: category/[slug].astro 카테고리 동적 라우트 페이지 생성</name>
  <files>src/pages/category/[slug].astro</files>
  <action>
src/pages/category/[slug].astro 동적 라우트 페이지를 생성한다.

**getStaticPaths():**
- 4개 카테고리 메타데이터 정의: { finance: '금융'/설명, marketing: '마케팅'/설명, hr: 'HR'/설명, it: 'IT·DevOps'/설명 }
- getCollection('templates')로 전체 템플릿 조회
- 각 카테고리별 필터링: allTemplates.filter(t => t.data.category === slug)
- return: params(slug) + props(category 메타 + filtered templates)

**페이지 구조:**
- BaseLayout 래핑 (title: 카테고리명, breadcrumbs: [{ label: 카테고리명, href: /n8n-marketplace/category/{slug} }])
- 상단 헤더 섹션 (py-12): 카테고리명(h1, text-2xl md:text-3xl) + 설명 한 줄 + "템플릿 N개" 카운트 (사용자 결정)
- templates.length > 0일 때:
  - SortDropdown 컴포넌트 배치
  - 템플릿 그리드 (id="template-grid"): grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 (사용자 결정)
  - 각 카드를 data-difficulty, data-date, data-pricing 속성이 있는 div로 감싸기 (정렬용)
    - data-difficulty: beginner=1, intermediate=2, advanced=3
    - data-date: publishedAt.toISOString()
    - data-pricing: free=0, premium=1
  - TemplateCard에 props 전달: title, description, category, difficulty, pricing, nodeCount, estimatedMinutes, slug(=t.id)
- templates.length === 0일 때: EmptyCategory 컴포넌트 표시 (currentSlug={category.slug})

**주의사항:**
- 모든 href에 /n8n-marketplace/ prefix 포함 (base path 누락 = GitHub Pages 404)
- TemplateCard/SortDropdown/EmptyCategory import 경로: ../../components/catalog/
- BaseLayout import: ../../layouts/BaseLayout.astro
  </action>
  <verify>
    <automated>cd "D:/PARA/Resource/2026-zion-spring-retreat/n8n-maker" && npm run build 2>&1 | tail -20 && ls dist/category/finance/index.html dist/category/marketing/index.html dist/category/hr/index.html dist/category/it/index.html 2>&1</automated>
  </verify>
  <done>npm run build가 에러 없이 완료되고, dist/category/ 아래 4개 카테고리(finance, marketing, hr, it) 폴더에 index.html이 생성된다</done>
</task>

</tasks>

<verification>
1. `npm run build` 에러 없이 완료
2. dist/category/ 아래 finance/, marketing/, hr/, it/ 4개 디렉토리에 index.html 존재
3. finance, hr, it 카테고리 페이지에 TemplateCard 카드가 렌더링됨 (빌드 출력물에서 확인 가능)
4. marketing 카테고리 페이지에 EmptyCategory 안내가 렌더링됨
</verification>

<success_criteria>
- 4개 카테고리 정적 페이지가 빌드되어 /n8n-marketplace/category/{slug}에서 접근 가능
- 카드에 제목/설명/배지(카테고리+난이도+가격)/보조정보(노드수+시간) 모두 표시
- 빈 카테고리(마케팅)에 안내 메시지 + 다른 카테고리 링크 표시
- 정렬 드롭다운이 DOM에 존재하고 클라이언트 JS로 재정렬 로직이 포함됨
</success_criteria>

<output>
완료 후 `.planning/phases/02-카탈로그-랜딩-UI/02-01-SUMMARY.md` 생성
</output>
