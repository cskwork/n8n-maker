---
phase: 05-seo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/layouts/BaseLayout.astro
  - astro.config.mjs
  - public/robots.txt
  - public/og-image.png
  - .github/workflows/deploy.yml
  - src/pages/template/[...id].astro
autonomous: true
requirements:
  - SEO-01
  - SEO-02

must_haves:
  truths:
    - "모든 페이지에 og:title, og:description, og:image, og:url, og:locale 메타 태그가 존재한다"
    - "Twitter Card 메타 태그(twitter:card, twitter:title 등)가 존재한다"
    - "canonical URL이 각 페이지에 설정된다"
    - "naver-site-verification 메타 태그 플레이스홀더가 존재한다"
    - "빌드 시 sitemap-index.xml이 자동 생성된다"
    - "public/robots.txt가 올바른 sitemap URL을 포함한다"
    - "템플릿 상세 페이지에 SoftwareApplication JSON-LD가 포함된다"
  artifacts:
    - path: "src/layouts/BaseLayout.astro"
      provides: "OG 메타 태그, canonical, Twitter Card, naver-verification, JSON-LD slot"
      contains: "og:title"
    - path: "astro.config.mjs"
      provides: "@astrojs/sitemap 통합 + site URL 수정"
      contains: "sitemap"
    - path: "public/robots.txt"
      provides: "크롤러 허용 규칙 + sitemap URL"
      contains: "Sitemap:"
    - path: "public/og-image.png"
      provides: "공통 OG 이미지 (1200x630)"
    - path: ".github/workflows/deploy.yml"
      provides: "withastro/action v5 업그레이드"
      contains: "withastro/action"
    - path: "src/pages/template/[...id].astro"
      provides: "SoftwareApplication JSON-LD 구조화 데이터"
      contains: "application/ld+json"
  key_links:
    - from: "src/layouts/BaseLayout.astro"
      to: "모든 페이지"
      via: "Props (ogImage, ogType, jsonLd)"
      pattern: "og:title|og:description|og:image"
    - from: "astro.config.mjs"
      to: "빌드 결과물"
      via: "@astrojs/sitemap 통합"
      pattern: "sitemap"
    - from: "src/pages/template/[...id].astro"
      to: "BaseLayout jsonLd prop"
      via: "SoftwareApplication JSON-LD 객체 전달"
      pattern: "jsonLd"
---

<objective>
SEO 기반 인프라 구축 -- BaseLayout에 OG 메타 태그/canonical/Twitter Card/JSON-LD 지원을 추가하고, @astrojs/sitemap으로 sitemap.xml 자동 생성을 설정하며, robots.txt와 OG 이미지를 생성한다.

Purpose: 검색 엔진(구글 + 네이버)과 소셜 미디어에서 올바른 메타 정보로 노출되는 기반을 확보한다.
Output: OG 태그가 포함된 BaseLayout, sitemap 자동 생성 설정, robots.txt, OG 이미지, JSON-LD 구조화 데이터
</objective>

<execution_context>
@C:/Users/a/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/a/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-seo/05-CONTEXT.md
@.planning/phases/05-seo/05-RESEARCH.md

<interfaces>
<!-- BaseLayout 현재 Props 인터페이스 -->
From src/layouts/BaseLayout.astro:
```typescript
interface Props {
  title: string;
  description?: string;
  breadcrumbs?: Array<{ label: string; href: string }>;
}
```

<!-- 템플릿 상세 페이지에서 사용하는 데이터 필드 -->
From src/pages/template/[...id].astro:
```typescript
const { title, description, category, difficulty, pricing, estimatedMinutes, credentials, workflowJsonPath, nodeCount, price } = template.data;
```

<!-- 현재 Astro 설정 -->
From astro.config.mjs:
```javascript
site: 'https://username.github.io',
base: '/n8n-marketplace',
integrations: [mdx()],
```

<!-- 현재 GitHub Actions 워크플로우 -->
From .github/workflows/deploy.yml:
```yaml
uses: withastro/action@v3  # v5로 업그레이드 필요
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BaseLayout OG 확장 + sitemap/robots.txt + astro.config 업데이트</name>
  <files>src/layouts/BaseLayout.astro, astro.config.mjs, public/robots.txt, public/og-image.png, .github/workflows/deploy.yml</files>
  <action>
1. **@astrojs/sitemap 설치**: `npx astro add sitemap --yes` 실행. 실패 시 `npm install @astrojs/sitemap` 후 수동으로 astro.config.mjs에 sitemap() 추가.

2. **astro.config.mjs 수정**:
   - `site`를 `'https://cskwork.github.io'`로 변경 (사용자 결정)
   - `base`는 `'/n8n-marketplace'` 유지
   - `integrations`에 `sitemap()` 추가: `integrations: [mdx(), sitemap()]`
   - `import sitemap from '@astrojs/sitemap';` 추가

3. **BaseLayout.astro Props 확장**:
   - 기존 Props 인터페이스에 `ogImage?: string`, `ogType?: string`, `jsonLd?: Record<string, unknown>` 추가
   - 기본값: `ogImage = '/n8n-marketplace/og-image.png'`, `ogType = 'website'`
   - `<head>` 내부에 추가:
     - `<link rel="canonical" href={canonicalURL} />` (canonicalURL = `new URL(Astro.url.pathname, Astro.site)`)
     - OG 태그: og:type, og:title, og:description, og:image, og:url, og:locale(ko_KR), og:site_name
     - Twitter Card: twitter:card(summary_large_image), twitter:title, twitter:description, twitter:image
     - 네이버 인증: `<meta name="naver-site-verification" content="NAVER_VERIFICATION_CODE_HERE" />` (TODO 코멘트 포함)
     - JSON-LD: `{jsonLd && (<script type="application/ld+json" set:html={JSON.stringify(jsonLd)} />)}`
   - title 패턴: `{title} | n8n 마켓플레이스` (기존 패턴 유지, 사용자 결정)
   - og:image URL은 `new URL(ogImage, Astro.site)` 패턴으로 base path 자동 처리

4. **public/robots.txt 생성**:
   ```
   User-agent: *
   Allow: /

   Sitemap: https://cskwork.github.io/n8n-marketplace/sitemap-index.xml
   ```

5. **public/og-image.png 생성**: 1200x630 PNG 이미지. 간단한 SVG를 HTML Canvas로 렌더링하거나, 단색 배경(n8n 오렌지 #f97316) + 흰색 텍스트 "n8n 업무 자동화 템플릿 마켓플레이스"를 포함한 최소 이미지 생성. Node.js 스크립트로 Canvas 없이 생성이 어려우면, 1x1 PNG를 플레이스홀더로 두고 TODO 코멘트로 교체 안내.

6. **.github/workflows/deploy.yml 업데이트**: `withastro/action@v3` -> `withastro/action@v5`로 변경. 나머지 구조는 동일 유지.
  </action>
  <verify>
    <automated>cd /d/PARA/Resource/2026-zion-spring-retreat/n8n-maker && npm run build 2>&1 | tail -20 && test -f dist/sitemap-index.xml && echo "SITEMAP OK" && test -f dist/robots.txt && echo "ROBOTS OK" && grep -l "og:title" dist/index.html && echo "OG OK"</automated>
  </verify>
  <done>
    - npm run build 에러 없이 완료
    - dist/sitemap-index.xml 파일 생성됨
    - dist/robots.txt 파일 존재 (Sitemap URL 포함)
    - dist/index.html에 og:title, og:description, og:image 메타 태그 포함
    - dist/index.html에 naver-site-verification 메타 태그 포함
    - dist/index.html에 canonical link 포함
    - public/og-image.png 파일 존재
    - .github/workflows/deploy.yml에 withastro/action@v5 사용
  </done>
</task>

<task type="auto">
  <name>Task 2: 템플릿 상세 페이지 JSON-LD 구조화 데이터</name>
  <files>src/pages/template/[...id].astro</files>
  <action>
1. **template/[...id].astro에 JSON-LD 객체 생성**:
   - frontmatter 영역에서 SoftwareApplication JSON-LD 객체를 구성:
     ```javascript
     const jsonLd = {
       "@context": "https://schema.org",
       "@type": "SoftwareApplication",
       "name": title,
       "description": description,
       "applicationCategory": "BusinessApplication",
       "operatingSystem": "Web",
       "inLanguage": "ko",
       "offers": {
         "@type": "Offer",
         "price": pricing === 'free' ? 0 : (price || 19000),
         "priceCurrency": "KRW",
       },
     };
     ```
   - BaseLayout에 `jsonLd` prop 전달: `<BaseLayout title={title} description={description} ogType="article" jsonLd={jsonLd} ...>`
   - 기존 breadcrumbs 등 다른 props는 그대로 유지

2. **상세 페이지 description 자동 활용**: 기존 MDX frontmatter의 `description` 필드가 이미 BaseLayout의 `description` prop으로 전달되고 있음 -- OG description도 자동으로 동일 값 사용 (사용자 결정)
  </action>
  <verify>
    <automated>cd /d/PARA/Resource/2026-zion-spring-retreat/n8n-maker && npm run build 2>&1 | tail -5 && grep -l "application/ld+json" dist/template/*/index.html 2>/dev/null | head -3 && echo "JSON-LD OK"</automated>
  </verify>
  <done>
    - 빌드 성공
    - 모든 템플릿 상세 페이지 HTML에 `<script type="application/ld+json">` 포함
    - JSON-LD 내용에 SoftwareApplication, name, offers.price 포함
    - 무료 템플릿은 price: 0, 프리미엄은 실제 가격 반영
  </done>
</task>

</tasks>

<verification>
- `npm run build` 에러 없이 완료
- `dist/sitemap-index.xml` 생성 확인
- `dist/robots.txt`에 올바른 Sitemap URL 포함
- `dist/index.html`에 og:title, og:image, canonical, naver-site-verification 메타 태그 존재
- 템플릿 상세 페이지에 application/ld+json 스크립트 존재
- `.github/workflows/deploy.yml`에 withastro/action@v5 사용
</verification>

<success_criteria>
1. 모든 빌드된 HTML 페이지에 OG 메타 태그(og:title, og:description, og:image, og:url, og:locale)가 포함된다
2. sitemap-index.xml이 빌드 결과물에 자동 생성된다
3. robots.txt가 올바른 sitemap URL을 포함한다
4. 템플릿 상세 페이지에 SoftwareApplication JSON-LD가 포함된다
5. 네이버 인증 메타 태그 플레이스홀더가 모든 페이지에 존재한다
</success_criteria>

<output>
After completion, create `.planning/phases/05-seo/05-01-SUMMARY.md`
</output>
