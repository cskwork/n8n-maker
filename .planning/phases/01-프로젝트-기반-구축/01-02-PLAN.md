---
phase: 01-프로젝트-기반-구축
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/layouts/BaseLayout.astro
  - src/components/layout/Header.astro
  - src/components/layout/Footer.astro
  - src/components/layout/MobileMenu.astro
  - src/components/layout/Breadcrumb.astro
  - src/pages/index.astro
autonomous: false
requirements:
  - INFRA-03

must_haves:
  truths:
    - "Header에 왼쪽 로고, 중앙 카테고리 4개 링크(금융/마케팅/HR/IT), 오른쪽 CTA 버튼이 표시된다"
    - "모바일(375px)에서 햄버거 메뉴로 전환되고, 클릭 시 네비게이션이 토글된다"
    - "Footer에 저작권, 문의 이메일/링크, 간단한 소개 문구가 표시된다"
    - "모든 페이지에서 BaseLayout이 Head, Header, Footer를 포함한다"
    - "375px/768px/1280px 뷰포트에서 레이아웃이 깨지지 않는다"
  artifacts:
    - path: "src/layouts/BaseLayout.astro"
      provides: "공통 레이아웃 (HTML head, Header, main slot, Footer)"
      min_lines: 20
    - path: "src/components/layout/Header.astro"
      provides: "로고 + 카테고리 네비게이션 + CTA + 햄버거 메뉴"
      min_lines: 30
    - path: "src/components/layout/Footer.astro"
      provides: "미니말 푸터 (저작권, 문의, 소개)"
      min_lines: 10
    - path: "src/components/layout/MobileMenu.astro"
      provides: "모바일 네비게이션 메뉴 (토글)"
      min_lines: 15
    - path: "src/pages/index.astro"
      provides: "BaseLayout을 사용하는 인덱스 페이지"
  key_links:
    - from: "src/layouts/BaseLayout.astro"
      to: "src/components/layout/Header.astro"
      via: "import + 렌더링"
      pattern: "import Header"
    - from: "src/layouts/BaseLayout.astro"
      to: "src/components/layout/Footer.astro"
      via: "import + 렌더링"
      pattern: "import Footer"
    - from: "src/layouts/BaseLayout.astro"
      to: "src/styles/global.css"
      via: "import"
      pattern: "import.*global.css"
    - from: "src/pages/index.astro"
      to: "src/layouts/BaseLayout.astro"
      via: "BaseLayout 래핑"
      pattern: "import BaseLayout"
    - from: "src/components/layout/Header.astro"
      to: "src/components/layout/MobileMenu.astro"
      via: "모바일 메뉴 토글"
      pattern: "MobileMenu"
---

<objective>
BaseLayout, Header, Footer, MobileMenu 컴포넌트를 생성하고 반응형 레이아웃을 구축하여 모바일/태블릿/데스크톱에서 정상 동작하는 사이트 쉘을 완성한다.

Purpose: 이 레이아웃이 Phase 2-5의 모든 페이지(카탈로그, 상세, 랜딩)의 기반이 된다. 반응형이 깨지면 전체 사이트 UX가 무너진다.
Output: BaseLayout + Header(반응형 네비게이션) + Footer + 인덱스 페이지, 375px/768px/1280px에서 정상 표시
</objective>

<execution_context>
@C:/Users/a/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/a/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-프로젝트-기반-구축/01-CONTEXT.md
@.planning/phases/01-프로젝트-기반-구축/01-RESEARCH.md
@.planning/phases/01-프로젝트-기반-구축/01-01-SUMMARY.md

<interfaces>
<!-- Plan 01에서 생성된 파일들의 핵심 인터페이스 -->

From src/styles/global.css:
- Tailwind CSS 4 @theme 블록에 --color-primary-*, --color-surface-*, --font-sans 정의됨
- Pretendard Variable 폰트 CDN import됨
- --color-bg, --color-text, --color-text-secondary CSS 변수 사용 가능

From src/content.config.ts:
- templates 컬렉션: title, description, category, useCase, difficulty, pricing, estimatedMinutes, credentials, tags, workflowJsonPath, nodeCount, publishedAt, updatedAt
- getCollection('templates')로 쿼리 가능
- entry.id로 접근 (entry.slug 아님 — Astro 5)

From astro.config.mjs:
- base: '/n8n-marketplace' 설정됨 (링크에 base path 반영 필요)
- MDX + Tailwind Vite 플러그인 설정됨
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BaseLayout + Header + Footer + MobileMenu 컴포넌트 생성</name>
  <files>src/layouts/BaseLayout.astro, src/components/layout/Header.astro, src/components/layout/Footer.astro, src/components/layout/MobileMenu.astro, src/components/layout/Breadcrumb.astro</files>
  <action>
1. src/layouts/BaseLayout.astro 생성:
   - Props: `{ title: string; description?: string; breadcrumbs?: Array<{label: string; href: string}> }`
   - HTML lang="ko", charset UTF-8, viewport meta
   - description 기본값: "n8n 워크플로우 템플릿 마켓플레이스 - 비개발자를 위한 업무 자동화"
   - title 포맷: `{title} | n8n 마켓플레이스`
   - global.css import
   - body: min-h-screen flex flex-col, bg/text는 CSS 변수 사용
   - 구조: Header → (Breadcrumb if breadcrumbs prop) → main.flex-1 > slot → Footer

2. src/components/layout/Header.astro 생성 — CONTEXT.md locked decisions 정확히 반영:
   - sticky top-0 z-50 bg-white border-b
   - 왼쪽: 로고 "n8n Market" (font-bold, text-primary-600, 홈 링크)
   - 중앙: 카테고리 4개 링크 — 금융(/category/finance), 마케팅(/category/marketing), HR(/category/hr), IT/DevOps(/category/it)
   - 오른쪽: CTA 버튼 "시작하기" (bg-primary-500, text-white, rounded-lg)
   - 데스크톱(md 이상): 카테고리 + CTA 표시, 햄버거 숨김
   - 모바일(md 미만): 카테고리 + CTA 숨김, 햄버거 버튼 표시
   - 현재 페이지 경로 active 상태는 Phase 2에서 구현 (지금은 기본 스타일만)

3. src/components/layout/MobileMenu.astro 생성:
   - id="mobile-menu", 기본 hidden, md:hidden
   - 카테고리 4개 링크 + CTA "시작하기" 버튼 (세로 배치)
   - 인라인 script로 토글 기능 구현: #menu-toggle 클릭 시 #mobile-menu hidden 토글
   - 메뉴 바깥 클릭 시 닫기는 Phase 2에서 (지금은 토글만)

4. src/components/layout/Footer.astro 생성 — CONTEXT.md 미니말 스타일:
   - max-w-7xl mx-auto, border-t
   - 저작권: "(c) 2026 n8n 마켓플레이스"
   - 문의: 이메일 링크 (placeholder: contact@example.com)
   - 간단한 소개: "비개발자를 위한 n8n 워크플로우 템플릿"

5. src/components/layout/Breadcrumb.astro 생성:
   - Props: `{ items: Array<{label: string; href: string}> }`
   - "홈" 항목은 항상 첫 번째
   - " > " 구분자로 연결
   - 마지막 항목은 링크 없이 현재 위치 표시 (font-semibold)
   - CONTEXT.md: 카테고리/상세 페이지에서만 표시 (BaseLayout이 breadcrumbs prop 유무로 판단)

주의사항:
- 모든 내부 링크에 Astro base path 고려 필요. 단, Astro는 `<a href="/category/finance">` 형태에서 자동으로 base를 추가하지 않으므로, 추후 통합 시 주의. Phase 1에서는 상대 경로로 작성하고 Phase 2에서 base path 유틸리티 추가.
- Tailwind CSS 4에서 커스텀 컬러는 `text-primary-600` 형태로 사용 가능 (--color-primary-600이 @theme에 정의되어 있으므로)
- v1은 라이트 모드만 지원 (다크모드 관련 클래스 사용 금지, 변수만 준비됨)
  </action>
  <verify>
    <automated>cd "D:/PARA/Resource/2026-zion-spring-retreat/n8n-maker" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>모든 레이아웃/컴포넌트 파일이 생성되고, npm run build가 성공한다. Header에 4개 카테고리 링크, CTA 버튼, 햄버거 버튼이 포함되고, Footer에 저작권+문의+소개가 포함된다.</done>
</task>

<task type="auto">
  <name>Task 2: 인덱스 페이지 BaseLayout 적용 + 반응형 확인용 콘텐츠</name>
  <files>src/pages/index.astro</files>
  <action>
1. src/pages/index.astro를 BaseLayout 기반으로 재작성:
   - BaseLayout title="홈"
   - 히어로 섹션: "n8n 워크플로우 마켓플레이스" 제목(text-3xl md:text-5xl font-bold), "비개발자를 위한 업무 자동화 템플릿" 부제(text-lg text-surface-700), CTA 버튼
   - 카테고리 미리보기 섹션: 4개 카테고리(금융/마케팅/HR/IT) 카드를 2x2 grid(md:grid-cols-2 lg:grid-cols-4)로 표시
   - 각 카테고리 카드: 카테고리 이름(한글) + 간단한 설명 + 해당 카테고리 링크
   - 샘플 템플릿 섹션: getCollection('templates')로 가져온 3개 템플릿의 title, category, difficulty, pricing을 간단한 리스트로 표시

2. 반응형 디자인 확인 포인트:
   - 히어로: 모바일에서 text-3xl, 데스크톱에서 text-5xl
   - 카테고리 그리드: 모바일 1col, 태블릿 2col, 데스크톱 4col
   - 전체 컨테이너: max-w-7xl mx-auto px-4 sm:px-6 lg:px-8
   - 여백과 패딩이 모바일에서도 충분 (px-4 최소)

3. 클린 프로페셔널 톤 반영:
   - 넓은 여백 (py-16 md:py-24 섹션 간격)
   - 명확한 텍스트 위계 (제목 > 부제 > 본문)
   - primary 컬러는 CTA와 강조 요소에만 사용
   - surface 컬러로 배경/텍스트 톤 조절
  </action>
  <verify>
    <automated>cd "D:/PARA/Resource/2026-zion-spring-retreat/n8n-maker" && npm run build && echo "Build OK" && ls dist/index.html</automated>
  </verify>
  <done>인덱스 페이지가 BaseLayout을 사용하고, Header/Footer가 포함되며, 4개 카테고리 카드 + 3개 샘플 템플릿이 표시된다. npm run build가 성공한다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 반응형 레이아웃 시각 검증</name>
  <action>
사용자가 브라우저에서 3개 뷰포트(375px, 768px, 1280px)로 레이아웃 정상 동작을 확인한다.
  </action>
  <what-built>BaseLayout + Header(반응형 네비게이션) + Footer + 인덱스 페이지의 완전한 사이트 쉘</what-built>
  <how-to-verify>
    1. `npm run preview` 실행 후 브라우저에서 http://localhost:4321/n8n-marketplace/ 접속
    2. 데스크톱(1280px): Header에 로고/카테고리 4개 링크/CTA 버튼 확인, 카테고리 그리드 4열 확인
    3. 태블릿(768px): DevTools로 뷰포트 변경, 카테고리 그리드 2열 확인
    4. 모바일(375px): 햄버거 메뉴 표시 확인, 클릭 시 모바일 메뉴 토글 확인, 카테고리 그리드 1열 확인
    5. Footer에 저작권/문의/소개 문구 확인
    6. Pretendard 폰트가 적용되어 한글이 깔끔하게 표시되는지 확인
    7. 전체적으로 클린 프로페셔널 톤이 유지되는지 확인 (넓은 여백, 명확한 위계, n8n 오렌지 컬러)
  </how-to-verify>
  <verify>사용자가 시각적으로 확인</verify>
  <done>사용자가 "approved"를 입력하거나 수정 사항이 반영됨</done>
  <resume-signal>"approved" 또는 수정 필요 사항을 설명</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` 성공
2. dist/index.html에 Header, Footer, 카테고리 카드, 템플릿 목록이 포함됨
3. 375px/768px/1280px 세 가지 뷰포트에서 레이아웃이 깨지지 않음
4. 모바일 햄버거 메뉴가 정상 토글됨
</verification>

<success_criteria>
- BaseLayout, Header, Footer, MobileMenu, Breadcrumb 컴포넌트가 모두 생성됨
- 인덱스 페이지가 BaseLayout을 사용하고 Content Collections 데이터를 표시함
- 모바일(375px): 햄버거 메뉴, 1열 그리드
- 태블릿(768px): 카테고리 네비게이션, 2열 그리드
- 데스크톱(1280px): 전체 네비게이션 + CTA, 4열 그리드
- Pretendard 폰트 적용, n8n 브랜드 컬러 반영
- npm run build 성공
</success_criteria>

<output>
완료 후 `.planning/phases/01-프로젝트-기반-구축/01-02-SUMMARY.md` 생성
</output>
